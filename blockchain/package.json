{
  "name": "farmguard-blockchain",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat project for FarmGuard's tamper-evident audit smart contract.",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
